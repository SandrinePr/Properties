# Node modules (deze zijn erg groot en worden via npm install binnengehaald)
node_modules/
dist/
dist-ssr/
*.local

# Omgevingsvariabelen (bevatten vaak API keys of wachtwoorden)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vite/Build bestanden
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor specifieke bestanden (VS Code, IntelliJ, etc)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS specifieke bestanden
.DS_Store
thumbs.db
desktop.ini

# WordPress specifieke zaken (indien je de hele WP folder in git hebt)
# Als je alleen je theme in git hebt, kun je deze negeren
wp-config.php
wp-content/uploads/
wp-content/upgrade/
wp-content/backup-db/